import { FilterPlacesByTypePipe } from './filter-places-by-type.pipe';

describe('FilterPlacesByTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPlacesByTypePipe();
    expect(pipe).toBeTruthy();
  });
});
